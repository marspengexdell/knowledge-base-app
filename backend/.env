PROJECT_NAME=Knowledge Base API
ENV=development

# gRPC 服务器配置（推荐主机和端口分开，利于拼接和部署管理）
GRPC_SERVER_HOST=localhost
GRPC_SERVER_PORT=50051

# 若要兼容原有写法（保留整体字符串形式）也可加一行
GRPC_SERVER=localhost:50051

# 后端 CORS 源（用逗号分隔字符串，pydantic 支持自动 split）
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:8081

# 可选：推理服务的 URL
INFERENCE_SERVER_URL=localhost:50051
